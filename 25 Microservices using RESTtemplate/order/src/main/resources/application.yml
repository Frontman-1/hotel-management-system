spring:
  datasource:
    url: jdbc:mysql://localhost:3306/order_service_microservices
    username: root
    password: 1538
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
  application:
    name: order_service

server:
  port: 8082

management:
  health:
    circuitbreakers:
      enabled: true
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: always

hystrix:
  dashboard:
    proxy-stream-allow-list: "*"